<%= form_with model: @event do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.file_field :photo %>
  <%= f.submit %>

<%= link_to "Delete",
  edit_event_path(@event),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
<% end %>